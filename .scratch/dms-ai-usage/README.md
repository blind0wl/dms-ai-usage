# Moved

This effort's tracker is now GitHub Issues on `blind0wl/dms-ai-usage`.

- Map: [#7](https://github.com/blind0wl/dms-ai-usage/issues/7)
- Spec: [#22](https://github.com/blind0wl/dms-ai-usage/issues/22)

The 14 ticket files that used to live in `issues/` are GitHub issues now. The map
lists them, each is a native sub-issue of it, and each closed ticket carries its
answer as a comment. Nothing was dropped, and the old files remain in git
history.

This directory is kept as a signpost only, so the old paths do not read as a live
tracker. The engineering skills read the GitHub issues, per
`docs/agents/issue-tracker.md`.
