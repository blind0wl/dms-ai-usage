## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `blind0wl/dms-ai-usage`, driven with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, as GitHub labels named for each role. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
